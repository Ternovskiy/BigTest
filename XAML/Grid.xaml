<Window x:Class="XAML.Grid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XAML"
        mc:Ignorable="d"
        Title="Grid" Height="300" Width="300">
    <Grid>


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"></ColumnDefinition>
            <ColumnDefinition Width="100">

            </ColumnDefinition>

        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="200"></RowDefinition>
            <RowDefinition Height="200"></RowDefinition>
        </Grid.RowDefinitions>


        <Button Width="100" Height="100" Content="test button2" Grid.Column="1" Grid.Row="1"/>
        <Button Width="100" Height="100" HorizontalAlignment="Left" VerticalAlignment="Bottom" >
            <Button.ToolTip>
                <Image Source="image.jpg"></Image>
                
            </Button.ToolTip>
            
            Buttun test</Button>


    </Grid>
</Window>
